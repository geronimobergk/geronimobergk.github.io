{% assign has_actions = false %}
{% if page.github or page.paper or page.pdf or page.demo or page.dataset or page.slides or page.video or page.doi %}
  {% assign has_actions = true %}
{% endif %}

{% if has_actions %}
  <div class="project-page__actions" aria-label="Project links">
    {% if page.github %}
      <a class="project-page__action project-page__action--github" href="{{ page.github }}" target="_blank" rel="noopener noreferrer" aria-label="Code repository">Code</a>
    {% endif %}
    {% if page.paper %}
      {% if page.paper contains '://' %}
        {% assign paper_href = page.paper %}
      {% else %}
        {% assign paper_href = page.paper | relative_url %}
      {% endif %}
      <a class="project-page__action" href="{{ paper_href }}" target="_blank" rel="noopener noreferrer" aria-label="Project paper">Paper</a>
    {% endif %}
    {% if page.pdf %}
      {% if page.pdf contains '://' %}
        {% assign pdf_href = page.pdf %}
      {% else %}
        {% assign pdf_href = page.pdf | relative_url %}
      {% endif %}
      <a class="project-page__action" href="{{ pdf_href }}" target="_blank" rel="noopener noreferrer" aria-label="Project PDF">PDF</a>
    {% endif %}
    {% if page.demo %}
      {% if page.demo contains '://' %}
        {% assign demo_href = page.demo %}
      {% else %}
        {% assign demo_href = page.demo | relative_url %}
      {% endif %}
      <a class="project-page__action" href="{{ demo_href }}" target="_blank" rel="noopener noreferrer" aria-label="Project demo">Demo</a>
    {% endif %}
    {% if page.dataset %}
      {% if page.dataset contains '://' %}
        {% assign dataset_href = page.dataset %}
      {% else %}
        {% assign dataset_href = page.dataset | relative_url %}
      {% endif %}
      <a class="project-page__action" href="{{ dataset_href }}" target="_blank" rel="noopener noreferrer" aria-label="Project dataset">Dataset</a>
    {% endif %}
    {% if page.slides %}
      {% if page.slides contains '://' %}
        {% assign slides_href = page.slides %}
      {% else %}
        {% assign slides_href = page.slides | relative_url %}
      {% endif %}
      <a class="project-page__action" href="{{ slides_href }}" target="_blank" rel="noopener noreferrer" aria-label="Project slides">Slides</a>
    {% endif %}
    {% if page.video %}
      {% if page.video contains '://' %}
        {% assign video_href = page.video %}
      {% else %}
        {% assign video_href = page.video | relative_url %}
      {% endif %}
      <a class="project-page__action" href="{{ video_href }}" target="_blank" rel="noopener noreferrer" aria-label="Project video">Video</a>
    {% endif %}
    {% if page.doi %}
      {% if page.doi contains '://' %}
        {% assign doi_href = page.doi %}
      {% else %}
        {% assign doi_href = 'https://doi.org/' | append: page.doi %}
      {% endif %}
      <a class="project-page__action" href="{{ doi_href }}" target="_blank" rel="noopener noreferrer" aria-label="Project DOI">DOI</a>
    {% endif %}
  </div>
{% endif %}
