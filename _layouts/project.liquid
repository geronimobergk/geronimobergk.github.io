---
layout: default
---
{% if page._styles %}
  <!-- Page/Post style -->
  <style type="text/css">
    {{ page._styles }}
  </style>
{% endif %}

<div class="project project-page">
  <header class="project-header">
    <a class="project-breadcrumb" href="{{ '/projects/' | relative_url }}">&larr; Projects</a>
    {% if page.description %}
      <p class="project-kicker">{{ page.description }}</p>
    {% endif %}
    <h3 class="project-title project-title--page">{{ page.title }}</h3>
    {% include project_actions.liquid %}
    {% if page.large_img %}
      {% assign hero_alt = page.img_alt | default: page.title %}
      <div class="project-hero">
        {% include figure.liquid loading="eager" path=page.large_img sizes="(min-width: 992px) 900px, 95vw" alt=hero_alt class="project-hero__image" caption=page.img_caption %}
      </div>
    {% endif %}
  </header>

  <article class="project-content">
    {{ content }}
  </article>

  {% if page.related_publications %}
    <h3 class="project-section-headline">References</h3>
    <div class="publications">
      {% bibliography --cited_in_order %}
    </div>
  {% endif %}

  {% if site.giscus and page.giscus_comments %}
    {% include giscus.liquid %}
  {% endif %}
</div>
